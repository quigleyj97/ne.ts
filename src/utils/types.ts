// A set of utility types
// These self-document what the expected bit size of each value is, and allow
// more direct copy-pastes from the original Rust prototype

export type u8 = number;
export type u16 = number;
